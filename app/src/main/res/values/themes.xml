<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.NoodoeTest" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <item name="windowNoTitle">true</item>
        <!-- Primary brand color. -->
        <item name="colorPrimary">?attr/colorText</item>
        <item name="colorPrimaryVariant">?attr/colorText</item>
        <item name="colorOnPrimary">?attr/colorText</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">?attr/colorText</item>
        <item name="colorSecondaryVariant">?attr/colorText</item>
        <item name="colorOnSecondary">?attr/colorText</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="colorText">@color/black</item>
        <item name="listBackground">@color/white</item>
    </style>
</resources>